DEBUG=True
SECRET_KEY=replace-this-with-a-random-string-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_NAME=playto
DB_USER=playto
DB_PASSWORD=playto
DB_HOST=localhost
DB_PORT=5432

# Redis (for Celery broker)
REDIS_URL=redis://localhost:6379/0

# CORS — comma-separated origins allowed to hit this API from a browser
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
